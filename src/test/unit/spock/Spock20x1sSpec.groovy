package spock

import spock.lang.Specification

class Spock20x1sSpec extends Specification {

    public static final int SLEEP_MILLISECONDS = 1000

    def 'demo spec 1'() {
        given:
        sleep(SLEEP_MILLISECONDS)
        expect:
        true
    }

    def 'demo spec 2'() {
        given:
        sleep(SLEEP_MILLISECONDS)
        expect:
        true
    }

    def 'demo spec 3'() {
        given:
        sleep(SLEEP_MILLISECONDS)
        expect:
        true
    }

    def 'demo spec 4'() {
        given:
        sleep(SLEEP_MILLISECONDS)
        expect:
        true
    }

    def 'demo spec 5'() {
        given:
        sleep(SLEEP_MILLISECONDS)
        expect:
        true
    }

    def 'demo spec 6'() {
        given:
        sleep(SLEEP_MILLISECONDS)
        expect:
        true
    }

    def 'demo spec 7'() {
        given:
        sleep(SLEEP_MILLISECONDS)
        expect:
        true
    }

    def 'demo spec 8'() {
        given:
        sleep(SLEEP_MILLISECONDS)
        expect:
        true
    }

    def 'demo spec 9'() {
        given:
        sleep(SLEEP_MILLISECONDS)
        expect:
        true
    }

    def 'demo spec 10'() {
        given:
        sleep(SLEEP_MILLISECONDS)
        expect:
        true
    }

    def 'demo spec 11'() {
        given:
        sleep(SLEEP_MILLISECONDS)
        expect:
        true
    }

    def 'demo spec 12'() {
        given:
        sleep(SLEEP_MILLISECONDS)
        expect:
        true
    }

    def 'demo spec 13'() {
        given:
        sleep(SLEEP_MILLISECONDS)
        expect:
        true
    }

    def 'demo spec 14'() {
        given:
        sleep(SLEEP_MILLISECONDS)
        expect:
        true
    }

    def 'demo spec 15'() {
        given:
        sleep(SLEEP_MILLISECONDS)
        expect:
        true
    }

    def 'demo spec 16'() {
        given:
        sleep(SLEEP_MILLISECONDS)
        expect:
        true
    }

    def 'demo spec 17'() {
        given:
        sleep(SLEEP_MILLISECONDS)
        expect:
        true
    }

    def 'demo spec 18'() {
        given:
        sleep(SLEEP_MILLISECONDS)
        expect:
        true
    }

    def 'demo spec 19'() {
        given:
        sleep(SLEEP_MILLISECONDS)
        expect:
        true
    }

    def 'demo spec 20'() {
        given:
        sleep(SLEEP_MILLISECONDS)
        expect:
        true
    }
}
